# Moby check-config

Run `./fetch.sh` to download Docker/Moby's `contrib/check-config.sh` into this directory.

The downloaded script is treated as third-party source and is intentionally not required for the core `achost verify-config` command.
